name: Publish to NuGet

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Restore from public NuGet
        run: >
          dotnet restore Penghou.Hongxian.slnx
          --source https://api.nuget.org/v3/index.json
          --packages "${{ runner.temp }}/hongxian-nuget-packages"
          --force --force-evaluate --no-cache

      - name: Pack
        shell: bash
        run: |
          version=$(dotnet msbuild src/Penghou.Hongxian/Penghou.Hongxian.csproj -getProperty:Version -nologo)
          if [ "$GITHUB_REF_TYPE" = "tag" ]; then
            version="${GITHUB_REF_NAME#v}"
          fi
          echo "PACKAGE_VERSION=$version" >> "$GITHUB_ENV"
          for csproj in \
            src/Penghou.Hongxian/Penghou.Hongxian.csproj \
            src/Penghou.Hongxian.Sqlite/Penghou.Hongxian.Sqlite.csproj; do
            dotnet pack "$csproj" \
              --configuration Release \
              --no-restore \
              --output ./artifacts \
              --property:PackageVersion="$version"
          done

      - name: Restore and audit packed consumer
        shell: pwsh
        run: ./tools/verify_packed_consumer.ps1 -PackageDirectory artifacts -Version $env:PACKAGE_VERSION

      - name: NuGet login (OIDC -> temp API key)
        id: login
        uses: NuGet/login@v1
        with:
          user: ${{ secrets.NUGET_USER }}

      - name: Push packages to NuGet
        run: >
          dotnet nuget push ./artifacts/*.nupkg
          --source https://api.nuget.org/v3/index.json
          --api-key ${{ steps.login.outputs.NUGET_API_KEY }}
          --skip-duplicate

      - name: Push symbols to NuGet
        run: >
          dotnet nuget push ./artifacts/*.snupkg
          --source https://api.nuget.org/v3/index.json
          --api-key ${{ steps.login.outputs.NUGET_API_KEY }}
          --skip-duplicate
