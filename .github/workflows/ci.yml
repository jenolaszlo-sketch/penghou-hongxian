name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  build-test-pack:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Build
        run: dotnet build Penghou.Hongxian.slnx --configuration Release

      - name: Verify formatting
        run: dotnet format Penghou.Hongxian.slnx --verify-no-changes --no-restore

      - name: Test
        run: dotnet test Penghou.Hongxian.slnx --configuration Release --no-build

      - name: Run standalone example
        run: dotnet run --project samples/Penghou.Hongxian.Example --configuration Release

      - name: Pack
        run: dotnet pack Penghou.Hongxian.slnx --configuration Release --no-build --output artifacts --property:PackageVersion=0.0.0-ci.${{ github.run_id }}

      - name: Upload packages
        uses: actions/upload-artifact@v6
        with:
          name: nuget-packages
          path: |
            artifacts/*.nupkg
            artifacts/*.snupkg
          if-no-files-found: error
